name: Release crates

on:
  push:
    branches: [main]
  # Resume a partially throttled crates.io publication without human input.
  # Scheduled workflows run from the repository's default branch (`main`).
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      command:
        description: Prepare a release PR, or publish packages ready for release
        required: true
        default: release-pr
        type: choice
        options:
          - release-pr
          - release

# release-plz creates or updates a Release PR for version changes. Pushes to
# main publish ready packages; scheduled runs resume a crates.io-throttled
# publication in dependency order without creating a Release PR.
jobs:
  release:
    name: Publish ready crates
    if: >-
      github.repository_owner == 'addlayerio' &&
      (github.event_name == 'push' || github.event_name == 'schedule' || inputs.command == 'release')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - name: Verify the release commit
        if: github.event_name != 'schedule'
        run: cargo test --workspace --all-features
      - name: Publish crates and create releases
        uses: release-plz/action@v0.5.131
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  release-pr:
    name: Prepare release PR
    if: >-
      github.repository_owner == 'addlayerio' &&
      (github.event_name == 'push' || inputs.command == 'release-pr')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - name: Create or update the release PR
        uses: release-plz/action@v0.5.131
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
